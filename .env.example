# Conexão com o Postgres (no Render vem preenchida automaticamente)
DATABASE_URL=postgresql://usuario:senha@localhost:5432/oren
# Porta do servidor (o Render define sozinho)
PORT=3000
# Deixe em production quando estiver publicado — liga o cookie seguro
NODE_ENV=development
