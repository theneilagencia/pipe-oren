{
  "name": "oren-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Painel comercial Oren — banco Postgres e login por pessoa",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "usuario": "node src/usuario.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
